//Not used :(
/*var queryInfo = {
    muted: false,
    url: ["*://*.facebook.com/*", "*://*.messenger.com/*"]
};

chrome.tabs.query(queryInfo, (tabs) =>{
    muteTabs(tabs);
});

function muteTabs(tabs){
    for(int i = 0; i < tabs.length; tabs++){
        chrome.tabs.update(tab[0].id, {muted: true});
    }
}
*/